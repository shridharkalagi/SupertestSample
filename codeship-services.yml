supertestsampleservice:
  build: 
    image: shridharkalagi/supertestsample
    context: .
    dockerfile: Dockerfile
    # args:
    #   build_env: production

data:
  image: dummy
  volumes:
    - ./package.json
supertestsampleservice:
  build: 
    image: shridharkalagi/supertestsample
    context: .
    dockerfile: Dockerfile
    # args:
    #   build_env: production

dummyservice:
  image: dummy
  volumes:
    - ./package.json